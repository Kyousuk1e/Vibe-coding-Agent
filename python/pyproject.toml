[project]
name = "minimal-agent-python"
version = "2.0.0"
description = "A framework-free Python Agent with real Qwen API, sessions, tools and bounded context"
requires-python = ">=3.11"
dependencies = []
